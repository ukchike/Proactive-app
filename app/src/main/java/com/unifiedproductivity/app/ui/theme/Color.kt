package com.unifiedproductivity.app.ui.theme

import androidx.compose.ui.graphics.Color

// Per-module accents, matched to the Apple apps they mimic:
// Notes = iOS gold (like iNotes/Apple Notes), Reminders = iOS blue, Calendar = iOS red.
val NotesAccent = Color(0xFFE0A800)
val RemindersAccent = Color(0xFF007AFF)
val CalendarAccent = Color(0xFFFF3B30)

// iOS grouped-list palette: light gray page with white cards…
val LightBackground = Color(0xFFF2F2F7)
val LightSurface = Color(0xFFFFFFFF)
val LightOnSurface = Color(0xFF000000)

// …and true-black page with elevated dark cards.
val DarkBackground = Color(0xFF000000)
val DarkSurface = Color(0xFF1C1C1E)
val DarkOnSurface = Color(0xFFFFFFFF)

// iOS system colors for priority indicators.
val PriorityHigh = Color(0xFFFF3B30)
val PriorityMedium = Color(0xFFFF9500)
val PriorityLow = Color(0xFF007AFF)
val AccentGreen = Color(0xFF34C759)
val AccentGray = Color(0xFF8E8E93)
